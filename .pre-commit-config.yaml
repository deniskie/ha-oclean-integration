repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.0
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(custom_components|tests)/
      - id: ruff-format
        files: ^(custom_components|tests)/
